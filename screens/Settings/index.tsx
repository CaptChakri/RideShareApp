import { ScrollView, Text, View } from "react-native";

const Settings = () => {
  return (
    <ScrollView>
      <View>
        <Text>Username</Text>
      </View>
    </ScrollView>
  );
};

export default Settings;
